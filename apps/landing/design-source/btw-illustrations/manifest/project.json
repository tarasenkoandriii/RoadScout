{
  "id": "beyond-the-wall-svg-design-system",
  "version": "5.2.0",
  "specification": "BTW SVG Design System v5.2",
  "license": "MIT",
  "tokens": "tokens.json",

  "assets": [
    { "id": "canonical-geometry",     "path": "01-Canonical-Geometry/" },
    { "id": "foundation",             "path": "02-Foundation/" },
    { "id": "svg-component-library",  "path": "03-SVG-Component-Library/", "note": "first-generation reference, superseded by hero/privacy, kept for history — see tooling/validation-report.md's legacy section" },
    { "id": "hero",                   "path": "04-Hero-Illustration/",   "manifest": "manifest/hero.json" },
    { "id": "icons",                  "path": "05-Icons/",               "manifest": "manifest/icons.json" },
    { "id": "privacy",                "path": "06-Privacy-Illustration/", "manifest": "manifest/privacy.json" },
    { "id": "og-image",               "path": "07-OpenGraph/",           "manifest": "manifest/og.json" },
    { "id": "favicon",                "path": "08-Favicon/",             "manifest": "manifest/favicon.json" },
    { "id": "previews",               "path": "09-Previews/" },
    { "id": "documentation",          "path": "10-Documentation/" }
  ],

  "schemas": {
    "location": "manifest/schemas/",
    "files": [
      "manifest/schemas/project.schema.json",
      "manifest/schemas/tokens.schema.json",
      "manifest/schemas/asset-manifest.schema.json"
    ],
    "commonManifestSchema": {
      "id": "string",
      "version": "string (SemVer X.Y.Z)",
      "viewBox": "[number, number, number, number]",
      "components": "string[]",
      "exports": "string[]",
      "tokens": "string (relative path to a tokens file)",
      "license": "string"
    },
    "note": "As of v5.2, this is backed by real JSON Schema files (Draft 2020-12), not just documentation prose. hero.json/privacy.json/icons.json/favicon.json/og.json all validate against asset-manifest.schema.json; tokens.json and project.json have their own dedicated schemas. Validated automatically by tooling/validate.py — see tooling/validation-report.md for the actual, current run output."
  },

  "validation": {
    "tool": "tooling/validate.py",
    "schemaValidator": "tooling/schema_validator.py",
    "report": "tooling/validation-report.md",
    "dependencies": "none (pure Python stdlib — the environment this was built in has no network access to install the real jsonschema package, so a minimal dependency-free validator covering the subset of JSON Schema this project's own schemas use was written instead)",
    "lastRunResult": "0 errors, 0 warnings, 16 accepted-legacy findings isolated to 03-SVG-Component-Library, 119 SVG files and 7 centralized + 6 per-folder manifests scanned"
  },

  "versioningNote": "This file and the other 6+3 in manifest/ (7 manifests, 3 schemas) use strict SemVer (X.Y.Z) per the v5.1 spec's acceptance criteria. The per-folder detailed manifests were written before that requirement existed and use shorter version strings ('1.0', '2.1') — those values are preserved as-is in their own files rather than silently rewritten; the SemVer-compliant equivalent is what appears in this centralized manifest/ folder instead.",

  "acceptanceCriteriaStatus": {
    "everyAssetHasItsOwnManifest": true,
    "exportsGeneratedOnlyFromMasterSvg": "true for hero/privacy/og/favicon (each has one master SVG per variant, exports are renders of it, not hand-edited separately); icons.json documents its own model (one master SVG per icon, no separate 'export' step needed since the source IS what ships)",
    "jsonSchemaCompatible": "true as of v5.2 — three real .schema.json files exist under manifest/schemas/, and tooling/validate.py actually validates every manifest against them on every run (see tooling/validation-report.md for the real, current output). Previously (v5.1) this field said schemas weren't shipped yet; that gap is now closed.",
    "semverVersioning": "true for the 10 files in manifest/ (7 manifests + 3 schemas); see versioningNote for the one place this doesn't reach backward into already-shipped per-folder manifests",
    "automatedValidation": "true as of v5.2 — tooling/validate.py is a real, runnable, dependency-free script checking manifest schema conformance, SVG structural rules (forbidden elements, required attributes, group nesting, duplicate IDs, color-token compliance), and file-size budgets, with output captured in tooling/validation-report.md. This is not yet wired into an actual CI/CD provider (no git host/CI config exists in this archive) — that remains real future work, but the validation logic itself is real, tested, and produces the report referenced above, not just a documentation claim."
  },

  "auditHistory": [
    {
      "date": "2026-08-03",
      "source": "BTW-v5-Audit-and-Recommendations.md",
      "findingsAddressed": [
        "No unified project.json → already existed prior to this audit (manifest/project.json), reconfirmed and updated in this pass to include 09-Previews/10-Documentation and the new schemas/tooling",
        "No JSON Schema for manifests → added: manifest/schemas/{project,tokens,asset-manifest}.schema.json, enforced by tooling/validate.py",
        "Unified Design Tokens → already existed prior to this audit (manifest/tokens.json), unchanged",
        "Hero as reference composition → already complete prior to this audit (5 variants, audited against the founding spec in an earlier pass)",
        "~70 SVG component/icon library → already complete prior to this audit (76 icons across 9 families in 05-Icons)",
        "SVG Sprite → already existed prior to this audit (05-Icons/sprites/)",
        "PNG/WebP/ICO export → already existed prior to this audit (07-OpenGraph has PNG/WebP/JPG, 08-Favicon has PNG/ICO)",
        "Contact sheets → already existed prior to this audit; the 05-Icons and 09-Previews copies were regenerated during this pass to fix a color-token inconsistency the validator caught (see tooling/validation-report.md and 10-Documentation/Changelog.md)",
        "Automated build/validation pipeline → partially addressed: tooling/validate.py is real and runs today; a full generate-from-manifest pipeline and CI/CD provider integration remain future work, not overclaimed as done"
      ],
      "note": "This audit's own 40-50% readiness estimate was computed against an earlier snapshot of the archive that predates 08-Favicon, 09-Previews, 10-Documentation, and the centralized manifest/ folder entirely — several of its 'missing' findings were already resolved by the time this audit was run. This auditHistory entry addresses the findings that were still genuinely open."
    }
  ]
}
